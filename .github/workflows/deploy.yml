name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and Redeploy
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.PRIVATE_KEY }}
          script: |
            cd farcaster-praise-bot
            git pull origin main
            /home/gmadmin/.nvm/versions/node/v20.16.0/bin/pm2 stop all
            /home/gmadmin/.nvm/versions/node/v20.16.0/bin/pm2 start --interpreter ~/.bun/bin/bun index.ts

